# Setup

Use command below to compile

```
mvn package
```

# Run the code
Go to the generated target folder and use the command

```
java -jar client-1.0-jar-with-dependencies.jar
```

If one wants to run the server locally, set the IP variable on the main class to localhost, and remove the method call for the dynamic server search.
Afterwards follow the setup section again
